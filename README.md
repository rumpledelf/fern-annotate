# Phrond Annotate

Phrond Annotate adds expressive text, arrows, and hand-drawn circles to photos entirely in the browser.

## Development

```bash
python3 app.py --port 8002
```

Then open `http://127.0.0.1:8002/`.
